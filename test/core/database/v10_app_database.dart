import 'package:drift/drift.dart';

final class V10AppDatabase extends GeneratedDatabase {
  V10AppDatabase(super.executor);

  @override
  int get schemaVersion => 10;

  @override
  Iterable<TableInfo> get allTables => const [];

  @override
  MigrationStrategy get migration => MigrationStrategy(
    onCreate: (_) async {
      for (final statement in v10SchemaStatements) {
        await customStatement(statement);
      }
      await customStatement(
        'INSERT INTO deadline_reminder_preferences '
        '(singleton_id) VALUES (1)',
      );
      await customStatement(
        'INSERT INTO deadline_reminder_reconciliations '
        '(singleton_id) VALUES (1)',
      );
      await customStatement(
        'INSERT INTO background_schedule_settings '
        '(singleton_id) VALUES (1)',
      );
      await customStatement(
        'INSERT INTO new_assignment_notification_preferences '
        '(singleton_id) VALUES (1)',
      );
    },
    beforeOpen: (_) => customStatement('PRAGMA foreign_keys = ON'),
  );
}

// Frozen from the physical schema created by commit 331c3b9. These statements
// intentionally do not import or reuse live production table definitions.
const v10SchemaStatements = <String>[
  '''CREATE TABLE "activities" ("semester_id" INTEGER NOT NULL, "identity_key" TEXT NOT NULL, "course_id" INTEGER NOT NULL, "backend_activity_id" INTEGER NULL, "user_id" INTEGER NOT NULL, "adv_starred" INTEGER NOT NULL, "group_type" TEXT NOT NULL, "activity_type" TEXT NOT NULL, "peer_assessment" INTEGER NOT NULL, "is_allow_repeat" INTEGER NOT NULL, "title" TEXT NOT NULL, "description" TEXT NOT NULL, "start_date_source" TEXT NULL, "due_date_source" TEXT NULL, "edit_group_mode" TEXT NOT NULL, "created_at_source" TEXT NOT NULL, "user_value" INTEGER NOT NULL, "activity_submission_id" INTEGER NULL, "class_user_id" INTEGER NOT NULL, "activity_group_id" INTEGER NULL, "activity_group_name" TEXT NULL, "activity_submission_submitted_at_json" TEXT NULL, "due_date_exceed" INTEGER NOT NULL CHECK ("due_date_exceed" IN (0, 1)), "quiz_submission_is_submitted" INTEGER NOT NULL CHECK ("quiz_submission_is_submitted" IN (0, 1)), "count_group_member" INTEGER NOT NULL, "activity_submission_is_late" INTEGER NOT NULL CHECK ("activity_submission_is_late" IN (0, 1)), "file_activities_json" TEXT NOT NULL, "questions_json" TEXT NOT NULL, "submissions_json" TEXT NOT NULL, "last_due_date_notification_date_source" TEXT NULL, "last_status_change_notification_date_source" TEXT NULL, "previous_submission_status" INTEGER NULL CHECK ("previous_submission_status" IN (0, 1)), PRIMARY KEY ("semester_id", "identity_key"), CHECK (length(trim(identity_key)) > 0), CHECK (course_id > 0), CHECK (backend_activity_id IS NULL OR backend_activity_id > 0), CHECK (length(trim(title)) > 0), FOREIGN KEY (semester_id, course_id) REFERENCES courses (semester_id, course_id) ON DELETE CASCADE)''',
  '''CREATE TABLE "activity_fingerprints" ("semester_id" INTEGER NOT NULL, "identity_key" TEXT NOT NULL, "fingerprint_version" INTEGER NOT NULL, "fingerprint" TEXT NOT NULL, PRIMARY KEY ("semester_id", "identity_key", "fingerprint_version"), CHECK (length(trim(identity_key)) > 0), CHECK (fingerprint_version > 0), CHECK (length(trim(fingerprint)) > 0), FOREIGN KEY (semester_id, identity_key) REFERENCES seen_activities (semester_id, identity_key) ON DELETE CASCADE)''',
  '''CREATE TABLE "app_settings" ("singleton_id" INTEGER NOT NULL, "active_semester_id" INTEGER NULL, "leb2_user_id" INTEGER NULL, "session_lifecycle" TEXT NOT NULL DEFAULT 'unknown', "session_revision" INTEGER NOT NULL DEFAULT 0, PRIMARY KEY ("singleton_id"), CHECK (singleton_id = 1), CHECK (active_semester_id IS NULL OR active_semester_id > 0), CHECK (leb2_user_id IS NULL OR (leb2_user_id > 0 AND leb2_user_id <= 2147483647)), CHECK (session_lifecycle IN ('unknown', 'active', 'expired')), CHECK (session_revision >= 0 AND session_revision <= 2147483647), FOREIGN KEY (active_semester_id) REFERENCES semesters (semester_id) ON DELETE SET NULL)''',
  '''CREATE TABLE "assignment_baselines" ("semester_id" INTEGER NOT NULL, "established_at_utc" INTEGER NULL, PRIMARY KEY ("semester_id"), CHECK (semester_id > 0), FOREIGN KEY (semester_id) REFERENCES semesters (semester_id) ON DELETE CASCADE)''',
  '''CREATE TABLE "background_schedule_settings" ("singleton_id" INTEGER NOT NULL, "monitoring_enabled" INTEGER NOT NULL DEFAULT 0 CHECK ("monitoring_enabled" IN (0, 1)), "install_jitter_seconds" INTEGER NULL, PRIMARY KEY ("singleton_id"), CHECK (singleton_id = 1), CHECK (install_jitter_seconds IS NULL OR (install_jitter_seconds >= 0 AND install_jitter_seconds <= 300)))''',
  '''CREATE TABLE "course_preferences" ("semester_id" INTEGER NOT NULL, "course_id" INTEGER NOT NULL, "notifications_muted" INTEGER NOT NULL DEFAULT 0 CHECK ("notifications_muted" IN (0, 1)), "background_monitoring_enabled" INTEGER NOT NULL DEFAULT 1 CHECK ("background_monitoring_enabled" IN (0, 1)), PRIMARY KEY ("semester_id", "course_id"), CHECK (semester_id > 0), CHECK (course_id > 0), FOREIGN KEY (semester_id) REFERENCES semesters (semester_id) ON DELETE CASCADE)''',
  '''CREATE TABLE "courses" ("semester_id" INTEGER NOT NULL, "course_id" INTEGER NOT NULL, "name" TEXT NOT NULL, PRIMARY KEY ("semester_id", "course_id"), CHECK (course_id > 0), CHECK (length(trim(name)) > 0), FOREIGN KEY (semester_id) REFERENCES semesters (semester_id) ON DELETE CASCADE)''',
  '''CREATE TABLE "deadline_reminder_preferences" ("singleton_id" INTEGER NOT NULL, "enabled" INTEGER NOT NULL DEFAULT 1 CHECK ("enabled" IN (0, 1)), "one_hour_enabled" INTEGER NOT NULL DEFAULT 1 CHECK ("one_hour_enabled" IN (0, 1)), "twenty_four_hours_enabled" INTEGER NOT NULL DEFAULT 1 CHECK ("twenty_four_hours_enabled" IN (0, 1)), PRIMARY KEY ("singleton_id"), CHECK (singleton_id = 1))''',
  '''CREATE TABLE "deadline_reminder_reconciliations" ("singleton_id" INTEGER NOT NULL, "requested_generation" INTEGER NOT NULL DEFAULT 0, "completed_generation" INTEGER NOT NULL DEFAULT 0, "owner_token" TEXT NULL, "lease_expires_at_utc" INTEGER NULL, "background_effects_only" INTEGER NOT NULL DEFAULT 0 CHECK ("background_effects_only" IN (0, 1)), PRIMARY KEY ("singleton_id"), CHECK (singleton_id = 1), CHECK (requested_generation >= 0 AND requested_generation <= 2147483647), CHECK (completed_generation >= 0 AND completed_generation <= requested_generation), CHECK ((owner_token IS NULL AND lease_expires_at_utc IS NULL) OR (owner_token IS NOT NULL AND lease_expires_at_utc IS NOT NULL AND length(trim(owner_token)) > 0)))''',
  '''CREATE TABLE "new_assignment_notification_preferences" ("singleton_id" INTEGER NOT NULL, "enabled" INTEGER NOT NULL DEFAULT 1 CHECK ("enabled" IN (0, 1)), PRIMARY KEY ("singleton_id"), CHECK (singleton_id = 1))''',
  '''CREATE TABLE "notification_history" ("dedupe_key" TEXT NOT NULL, "semester_id" INTEGER NOT NULL, "identity_key" TEXT NOT NULL, "kind" TEXT NOT NULL, "notification_id" INTEGER NOT NULL, "recorded_at_utc" INTEGER NOT NULL, PRIMARY KEY ("dedupe_key"), CHECK (length(trim(dedupe_key)) > 0), CHECK (length(trim(identity_key)) > 0), CHECK (length(trim(kind)) > 0), FOREIGN KEY (semester_id, identity_key) REFERENCES seen_activities (semester_id, identity_key) ON DELETE CASCADE)''',
  '''CREATE TABLE "scheduled_reminders" ("notification_id" INTEGER NOT NULL, "semester_id" INTEGER NOT NULL, "identity_key" TEXT NOT NULL, "offset_minutes" INTEGER NOT NULL, "deadline_at_utc" INTEGER NOT NULL, "scheduled_for_utc" INTEGER NOT NULL, "created_at_utc" INTEGER NOT NULL, "needs_reconciliation" INTEGER NOT NULL DEFAULT 1 CHECK ("needs_reconciliation" IN (0, 1)), "schedule_state" TEXT NOT NULL DEFAULT 'unknown', PRIMARY KEY ("notification_id"), CHECK (length(trim(identity_key)) > 0), CHECK (offset_minutes > 0), CHECK (schedule_state IN ('unknown', 'scheduled', 'cancelled')), CHECK ((schedule_state = 'unknown' AND needs_reconciliation = 1) OR (schedule_state IN ('scheduled', 'cancelled') AND needs_reconciliation = 0)), FOREIGN KEY (semester_id, identity_key) REFERENCES seen_activities (semester_id, identity_key) ON DELETE CASCADE)''',
  '''CREATE TABLE "seen_activities" ("semester_id" INTEGER NOT NULL, "identity_key" TEXT NOT NULL, "course_id" INTEGER NOT NULL, "first_seen_at_utc" INTEGER NOT NULL, "last_seen_at_utc" INTEGER NOT NULL, "is_baseline" INTEGER NOT NULL CHECK ("is_baseline" IN (0, 1)), PRIMARY KEY ("semester_id", "identity_key"), CHECK (length(trim(identity_key)) > 0), CHECK (course_id > 0), FOREIGN KEY (semester_id) REFERENCES semesters (semester_id) ON DELETE CASCADE)''',
  '''CREATE TABLE "semesters" ("semester_id" INTEGER NOT NULL, PRIMARY KEY ("semester_id"), CHECK (semester_id > 0))''',
  '''CREATE TABLE "sync_backoff_states" ("semester_id" INTEGER NOT NULL, "user_id" INTEGER NOT NULL, "consecutive_failure_count" INTEGER NOT NULL, "state" TEXT NOT NULL, "next_automatic_attempt_at_utc" INTEGER NULL, "last_failure_kind" TEXT NOT NULL, "last_failure_detail" TEXT NULL, "last_retry_after_milliseconds" INTEGER NULL, "updated_at_utc" INTEGER NOT NULL, PRIMARY KEY ("semester_id", "user_id"), CHECK (semester_id > 0), CHECK (user_id > 0), CHECK (consecutive_failure_count > 0), CHECK (state IN ('waiting', 'blocked')), CHECK (last_retry_after_milliseconds IS NULL OR last_retry_after_milliseconds >= 0), CHECK (last_failure_kind IN ('sessionExpired', 'networkUnavailable', 'requestTimeout', 'backendUnavailable', 'rateLimited', 'invalidResponse', 'unknown')), CHECK ((last_failure_kind = 'requestTimeout' AND last_failure_detail IS NOT NULL AND last_failure_detail IN ('connection', 'send', 'receive', 'transform', 'server') AND last_retry_after_milliseconds IS NULL) OR (last_failure_kind = 'unknown' AND last_failure_detail IS NOT NULL AND last_failure_detail IN ('missingCredential', 'credentialAccessFailed', 'badCertificate', 'authenticationRequired', 'invalidRequest', 'resourceNotFound', 'unexpectedServerFailure', 'unexpectedHttpResponse', 'unexpectedTransportFailure', 'persistenceFailed') AND last_retry_after_milliseconds IS NULL) OR (last_failure_kind IN ('sessionExpired', 'networkUnavailable', 'invalidResponse') AND last_failure_detail IS NULL AND last_retry_after_milliseconds IS NULL) OR (last_failure_kind IN ('backendUnavailable', 'rateLimited') AND last_failure_detail IS NULL)), CHECK ((state = 'waiting' AND next_automatic_attempt_at_utc IS NOT NULL) OR (state = 'blocked' AND next_automatic_attempt_at_utc IS NULL)), CHECK (state != 'waiting' OR last_failure_kind IN ('networkUnavailable', 'requestTimeout', 'backendUnavailable', 'rateLimited') OR (last_failure_kind = 'unknown' AND last_failure_detail IN ('unexpectedServerFailure', 'unexpectedTransportFailure'))), CHECK (state != 'blocked' OR last_failure_kind IN ('sessionExpired', 'invalidResponse') OR (last_failure_kind = 'unknown' AND last_failure_detail IN ('missingCredential', 'credentialAccessFailed', 'badCertificate', 'authenticationRequired', 'invalidRequest', 'resourceNotFound', 'unexpectedHttpResponse', 'persistenceFailed'))), FOREIGN KEY (semester_id) REFERENCES semesters (semester_id) ON DELETE CASCADE)''',
  '''CREATE TABLE "sync_operation_changes" ("operation_id" INTEGER NOT NULL, "semester_id" INTEGER NOT NULL, "identity_key" TEXT NOT NULL, "kind" TEXT NOT NULL, PRIMARY KEY ("operation_id", "identity_key", "kind"), CHECK (semester_id > 0), CHECK (length(trim(identity_key)) > 0), CHECK (kind IN ('newActivity', 'deadlineChanged', 'removed')), FOREIGN KEY (operation_id, semester_id) REFERENCES sync_operations (operation_id, semester_id) ON DELETE CASCADE, FOREIGN KEY (semester_id, identity_key) REFERENCES seen_activities (semester_id, identity_key) ON DELETE CASCADE)''',
  '''CREATE TABLE "sync_operations" ("operation_id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, "semester_id" INTEGER NOT NULL, "user_id" INTEGER NOT NULL, "reason" TEXT NOT NULL, "state" TEXT NOT NULL, "enqueued_at_utc" INTEGER NOT NULL, "started_at_utc" INTEGER NULL, "completed_at_utc" INTEGER NULL, "owner_token" TEXT NULL, "lease_expires_at_utc" INTEGER NULL, "cancellation_requested" INTEGER NOT NULL DEFAULT 0 CHECK ("cancellation_requested" IN (0, 1)), "session_revision" INTEGER NOT NULL DEFAULT 0, "result_failure_kind" TEXT NULL, "result_failure_detail" TEXT NULL, "result_retry_after_milliseconds" INTEGER NULL, "result_course_count" INTEGER NULL, "result_activity_count" INTEGER NULL, CHECK (semester_id > 0), CHECK (user_id > 0), CHECK (session_revision >= 0 AND session_revision <= 2147483647), CHECK (reason IN ('initialSetup', 'appLaunch', 'appResume', 'manualRefresh', 'backgroundTask', 'desktopTimer', 'trayAction')), CHECK (state IN ('queued', 'running', 'success', 'failure', 'cancelled')), CHECK (owner_token IS NULL OR length(trim(owner_token)) > 0), CHECK (result_retry_after_milliseconds IS NULL OR result_retry_after_milliseconds >= 0), CHECK (result_course_count IS NULL OR result_course_count >= 0), CHECK (result_activity_count IS NULL OR result_activity_count >= 0), CHECK (result_failure_kind IS NULL OR result_failure_kind IN ('sessionExpired', 'networkUnavailable', 'requestTimeout', 'backendUnavailable', 'rateLimited', 'invalidResponse', 'unknown')), CHECK ((result_failure_kind IS NULL AND result_failure_detail IS NULL AND result_retry_after_milliseconds IS NULL) OR (result_failure_kind = 'requestTimeout' AND result_failure_detail IS NOT NULL AND result_failure_detail IN ('connection', 'send', 'receive', 'transform', 'server') AND result_retry_after_milliseconds IS NULL) OR (result_failure_kind = 'unknown' AND result_failure_detail IS NOT NULL AND result_failure_detail IN ('missingCredential', 'credentialAccessFailed', 'cancelled', 'badCertificate', 'authenticationRequired', 'invalidRequest', 'resourceNotFound', 'unexpectedServerFailure', 'unexpectedHttpResponse', 'unexpectedTransportFailure', 'persistenceFailed') AND result_retry_after_milliseconds IS NULL) OR (result_failure_kind IN ('sessionExpired', 'networkUnavailable', 'invalidResponse') AND result_failure_detail IS NULL AND result_retry_after_milliseconds IS NULL) OR (result_failure_kind IN ('backendUnavailable', 'rateLimited') AND result_failure_detail IS NULL)), CHECK ((state = 'queued' AND owner_token IS NULL AND lease_expires_at_utc IS NULL AND completed_at_utc IS NULL AND result_failure_kind IS NULL AND result_failure_detail IS NULL AND result_retry_after_milliseconds IS NULL AND result_course_count IS NULL AND result_activity_count IS NULL) OR (state = 'running' AND owner_token IS NOT NULL AND started_at_utc IS NOT NULL AND lease_expires_at_utc IS NOT NULL AND completed_at_utc IS NULL AND result_failure_kind IS NULL AND result_failure_detail IS NULL AND result_retry_after_milliseconds IS NULL AND result_course_count IS NULL AND result_activity_count IS NULL) OR (state = 'success' AND owner_token IS NULL AND lease_expires_at_utc IS NULL AND completed_at_utc IS NOT NULL AND result_failure_kind IS NULL AND result_failure_detail IS NULL AND result_retry_after_milliseconds IS NULL AND result_course_count IS NOT NULL AND result_activity_count IS NOT NULL) OR (state = 'failure' AND owner_token IS NULL AND lease_expires_at_utc IS NULL AND completed_at_utc IS NOT NULL AND result_failure_kind IS NOT NULL AND result_course_count IS NULL AND result_activity_count IS NULL) OR (state = 'cancelled' AND owner_token IS NULL AND lease_expires_at_utc IS NULL AND completed_at_utc IS NOT NULL AND result_failure_kind IS NULL AND result_failure_detail IS NULL AND result_retry_after_milliseconds IS NULL AND result_course_count IS NULL AND result_activity_count IS NULL)), FOREIGN KEY (semester_id) REFERENCES semesters (semester_id) ON DELETE CASCADE)''',
  '''CREATE TABLE "sync_runs" ("sync_run_id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT, "semester_id" INTEGER NOT NULL, "reason" TEXT NOT NULL, "outcome" TEXT NOT NULL, "started_at_utc" INTEGER NOT NULL, "completed_at_utc" INTEGER NULL, "failure_category" TEXT NULL, CHECK (semester_id > 0), CHECK (length(trim(reason)) > 0), CHECK (length(trim(outcome)) > 0), FOREIGN KEY (semester_id) REFERENCES semesters (semester_id) ON DELETE CASCADE)''',
  '''CREATE UNIQUE INDEX activities_backend_identity ON activities (semester_id, backend_activity_id) WHERE backend_activity_id IS NOT NULL''',
  '''CREATE INDEX activities_by_course ON activities (semester_id, course_id)''',
  '''CREATE UNIQUE INDEX activity_fingerprints_by_value ON activity_fingerprints (semester_id, fingerprint_version, fingerprint)''',
  '''CREATE INDEX notification_history_by_assignment_kind ON notification_history (semester_id, identity_key, kind)''',
  '''CREATE UNIQUE INDEX scheduled_reminders_by_assignment_offset ON scheduled_reminders (semester_id, identity_key, offset_minutes)''',
  '''CREATE INDEX scheduled_reminders_by_scheduled_time ON scheduled_reminders (scheduled_for_utc)''',
  '''CREATE INDEX scheduled_reminders_pending_reconciliation ON scheduled_reminders (semester_id, identity_key) WHERE needs_reconciliation = 1''',
  '''CREATE INDEX seen_activities_by_course_and_last_seen ON seen_activities (semester_id, course_id, last_seen_at_utc)''',
  '''CREATE INDEX sync_backoff_states_by_next_attempt ON sync_backoff_states (state, next_automatic_attempt_at_utc, semester_id, user_id)''',
  '''CREATE UNIQUE INDEX sync_operations_one_active_key ON sync_operations (semester_id, user_id) WHERE state IN ('queued', 'running')''',
  '''CREATE UNIQUE INDEX sync_operations_one_running ON sync_operations (state) WHERE state = 'running' ''',
  '''CREATE UNIQUE INDEX sync_operations_operation_semester ON sync_operations (operation_id, semester_id)''',
  '''CREATE INDEX sync_operations_queue ON sync_operations (state, operation_id)''',
  '''CREATE INDEX sync_operations_terminal_cleanup ON sync_operations (completed_at_utc, operation_id)''',
  '''CREATE INDEX sync_runs_by_started_time ON sync_runs (started_at_utc DESC, sync_run_id DESC)''',
];
